services:
  db:
    container_name: db
    image: postgres:17-alpine
    environment:
      POSTGRES_SSL_MODE: "disable"
      POSTGRES_HOST: "db"
      POSTGRES_PORT: "5432"
      POSTGRES_DB: "db"
      POSTGRES_PASSWORD: "password"
      POSTGRES_USER: "user"
    ports:
      - "5432:5432"